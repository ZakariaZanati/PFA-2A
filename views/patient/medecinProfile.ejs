<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PFA</title>
    <link rel="stylesheet" href="public/css/style.css" type="text/css">
    <script src="https://kit.fontawesome.com/dff21dd380.js" crossorigin="anonymous"></script>
</head>

<body>
    <nav class="navigation-bar">
        <input type="checkbox" id="check">
        <label for="check" class="checkbtn"><i class="fas fa-bars"></i></label>
        <label class="logo">LOGO</label>
        <ul>
            <li><a href="patientHome">Accueil</a></li>
            <li><a href="#">Statistiques</a></li>
            <li><a class="active" href="medecins">Médecins</a></li>
            <li><a href="seuils">Seuils</a></li>
            <li><a href="#">Profil</a></li>
            <li><a href="/logout">Deconnexion</a></li>
        </ul>
    </nav>
    <div class="container">
        <div class="container-header container-img-header">
            <div class="overlay"></div>
            <img src="public/images/doctor.jpg" class="img-header">
            <h1 class="text-header">Les informations du médecin <span><%= medecin.nom %> <%= medecin.prenom %></span>
            </h1>
        </div>
        <div class="container-body">
            <div class="profil">
                <label>
                    <span>Nom complet</span>
                    <input class="login-input" type="text" name="fullname" value="<%= medecin.nom %> <%=medecin.prenom %>" readonly>
                </label>
                <label>
                    <span>Email</span>
                    <input class="login-input" type="text" name="email" value="<%= medecin.email %>" readonly>
                </label>
                <label>
                    <span>Téléphone</span>
                    <input class="login-input" type="number" name="phone" value="<%= medecin.telephone %>" readonly>
                </label>
                <label>
                    <span>Pays</span>
                    <input class="login-input" type="text" name="pays" value="<%= medecin.pays %>" readonly>
                </label>
                <label>
                    <span>Ville</span>
                    <input class="login-input" type="text" name="ville" value="<%= medecin.ville %>" readonly>
                </label>

                <label>
                    <span>Adresse du lieu de travail</span>
                    <input class="login-input" type="text" name="adresse" value="<%= medecin.adresse_lieu_travail %>"
                        readonly>

                </label>
                <% if(estMedecin == null) { %>
                    <% if(sentDemande) {%>
                        Demande nouveau contrat envoyée
                        <form action="/medecinProfile?id=<%= medecin._id%>&action=no" method="post"> 
                            <input class="btn btn-success" type="submit" value="Annuler la demande">
                            <a href="medecins" class="btn btn-echec">Retour</a>
                        </form>
                    <% } else if(sentDemande == false) { %>
                        <div class="buttons">
                            <form action="/medecinProfile?id=<%= medecin._id%>&action=yes" method="post">
                                <input class="btn btn-success" type="submit" value="Envoyer la demande">
                            </form>
                            <a href="medecins" class="btn btn-echec">Retour</a>
                        </div>
                        
                <% }} else if(estMedecin == true) { %> 
                    <label>
                        <span>Date debut contrat:</span>
                        <input class="login-input" type="text" name="adresse" value="<%= debutContrat %>"readonly>
                    </label>
                    <% if(sentDemande) {%>
                        Demande fin contrat envoyée
                        <form action="/medecinProfile?id=<%= medecin._id%>&action=no" method="post"> 
                            <input class="btn btn-success" type="submit" value="Annuler la demande">
                            <a href="medecins" class="btn btn-echec">Retour</a>
                        </form>
                    <% } else if(sentDemande == false) { %>
                        <form action="/medecinProfile?id=<%= medecin._id%>&action=end" method="post"> 
                            <input class="btn btn-success" type="submit" value="Fin contrat">
                            <a href="medecins" class="btn btn-echec">Retour</a>
                        </form>
                <% }} %>
            </div>
        </div>
    </div>


    </div>


</body>

<script src="public/js/sortedTable.js"></script>
<script src="public/js/app.js"></script>

</html>